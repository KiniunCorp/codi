export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>CODI Next.js Demo</h1>
      <p>
        This minimal application ships with an intentionally naive Dockerfile so that CODI
        can demonstrate optimisation strategies in later waves.
      </p>
    </main>
  );
}
